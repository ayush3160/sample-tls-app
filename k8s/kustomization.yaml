apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Apply with:
#   kubectl apply -k k8s/
# Tear down with:
#   kubectl delete -k k8s/

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
